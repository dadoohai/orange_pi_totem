# Central decision

Decision: **GO** for closing M9 items 1 and 2, with zero unresolved blockers.

The evidence audit approved the package and board claims. The focused code
audit then found a plausible race when the Python wizard is launched directly
twice. A separate full-path audit confirmed that the supported product path is
singleton and acquires its session lock before launching the wizard.

The direct privileged bypass remains a valid defense-in-depth improvement, but
it does not invalidate the tested product flow and is not a regression from
C21.20. No runtime change is being introduced after the exact package and board
roundtrip evidence.

Next functional priority: M9 item 3, ordinary open Wi-Fi networks.
